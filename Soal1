#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main() {
    system ("cls");
    char kartu[20];
    int i, panjang, digit, total = 0;

    printf("Masukkan nomor kartu digital: ");
    scanf("%s", kartu);

    panjang = strlen(kartu);

    if (panjang == 14 && kartu[0] == '6' && kartu[1] == '5') {
        printf("Tipe kartu: NUSANTARA\n");

        for (i = panjang - 2; i >= 0; i -= 2) {
            digit = (kartu[i] - '0') * 2;
            if (digit > 9) digit -= 9;
            total += digit;
        }
        for (i = panjang - 1; i >= 0; i -= 2) {
            total += (kartu[i] - '0');
        }

        if (total % 10 == 0) printf("Nomor kartu VALID.\n");
        else printf("Nomor kartu TIDAK VALID.\n");
    } else {
        printf("Nomor kartu tidak sesuai format kartu NUSANTARA.\n");
    }
    return 0;
}

/*
Penjelasan singkat:
- Program ini membaca input nomor kartu dari user.
- Diperiksa panjangnya 14 digit dan diawali dengan 65 untuk menentukan jenis NUSANTARA.
- Validasi dilakukan dengan algoritma: digit ke-2 dari belakang dikali 2 tiap selang 2 angka,
  jika hasil >9 dikurang 9, lalu dijumlahkan semua digit dan dicek habis dibagi 10 atau nggak.
- Output akan menyebut jenis kartu dan status valid/tidak.
*/

